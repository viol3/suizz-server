{
  "name": "wahoot-wss",
  "version": "1.0.0",
  "description": "Simple WebSocket server for wahoot in TypeScript using ws",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "dependencies": {
    "@mysten/enoki": "^0.12.1",
    "@mysten/sui": "1.30.0",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
